name: Release

# Builds signed-or-unsigned installers for every desktop platform and attaches
# them to a GitHub Release. macOS and Linux cannot be cross-compiled from a
# Windows machine, so this runs each target on its own native runner.
#
# Fork releases are manual, draft prereleases only. Upstream v* tags never
# trigger publishing. Enable wider release automation after stage 6 acceptance.
on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing fork-only tag (pi-v*, e.g. pi-v0.5.1-alpha.1)'
        required: true

permissions:
  contents: write

jobs:
  build:
    if: ${{ github.repository == 'chunkanglu/ccanvas' && startsWith(inputs.tag, 'pi-v') }}
    name: ${{ matrix.label }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # one universal binary → a single DMG that runs on Apple Silicon + Intel
          - platform: macos-latest
            label: macOS (universal)
            args: '--target universal-apple-darwin'
            rust-targets: 'aarch64-apple-darwin,x86_64-apple-darwin'
          - platform: ubuntu-22.04
            label: Linux (x86_64)
            args: ''
            rust-targets: ''
          - platform: windows-latest
            label: Windows (x86_64)
            args: ''
            rust-targets: ''
    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            build-essential \
            curl wget file \
            libxdo-dev \
            libssl-dev \
            libfuse2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build and publish
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Bundling the Monaco + hdf5 wasm chunks OOMs Node's default heap on
          # the ~7 GB macOS runner; raise it so `npm run build` doesn't crash.
          NODE_OPTIONS: --max-old-space-size=6144
          # linuxdeploy is itself an AppImage; the ubuntu-22.04 runner has no
          # FUSE, so tell it to extract-and-run instead of mounting (otherwise
          # "failed to run linuxdeploy" during AppImage bundling).
          APPIMAGE_EXTRACT_AND_RUN: 1
        with:
          tagName: ${{ inputs.tag }}
          releaseName: 'ccanvas Pi __VERSION__'
          releaseDraft: true
          prerelease: true
          args: ${{ matrix.args }}
